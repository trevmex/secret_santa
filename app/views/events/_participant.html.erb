<li>
  <%= check_box_tag('participants[]', participant.id, @event.participants.map { |p| p.id }.include?(participant.id)) unless in_form.blank? %>
  <%= participant.name %> <%= (" gets a gift from " + Participant.find(participant.participant_id).name) unless in_form.present? %>
</li>